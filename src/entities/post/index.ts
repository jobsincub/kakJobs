export { useCreatePostMutation, useDeletePostMutation, postApi } from './api/postApi'
export {
  postSlice,
  nextStep,
  previousStep,
  reset,
  setDescription,
  setPhoto,
  removePhoto,
  selectStep,
  selectPhotos,
  OrderStatus,
} from './model/postSlice'
